{
  "name": "mugen-ui-web-assets",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:json-editor": "esbuild tool/codemirror/acp_json_editor.js --bundle --format=iife --global-name=MugenAcpJsonEditorBundle --outfile=web/js/acp_json_editor.bundle.js --minify --target=es2020"
  },
  "dependencies": {
    "@codemirror/lang-json": "^6.0.2",
    "codemirror": "^6.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
